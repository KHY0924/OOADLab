import javax.management.openmbean.OpenDataException;
import javax.swing.*;
import java.awt.event.*;

public class StudentFrame extends JFrame {
    public StudentFrame () {
        super ("Student");
        setSize(400,600);
        setLocationRelativeTo(null);

        // Has register and upload page
    }
}